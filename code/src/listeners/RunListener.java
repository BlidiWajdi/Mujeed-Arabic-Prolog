/*package listeners;

import java.awt.Color;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import java.util.LinkedList;
import java.util.List;
import javax.swing.text.AbstractDocument;

import gui_tools.NonEditableLineDocumentFilter;
import front_end.FromGui;
import front_end.RunProgram;
import frames.PrincipalWindow;

public class RunListener implements ActionListener {
	
	public static List querys = new LinkedList();
	public static int position = 1;

	@Override
	public void actionPerformed(ActionEvent e) {
		// TODO Auto-generated method stub
		/*FromGui.clear();
		CompileListener.prepareAndCompile();
		((AbstractDocument)PrincipalWindow.interpreter.getDocument()).setDocumentFilter(new NonEditableLineDocumentFilter());
		FromGui.insert(PrincipalWindow.interpreter, "\n"+"؟-	", Color.BLACK);
		PrincipalWindow.interpreter.setCaretPosition(PrincipalWindow.interpreter.getDocument().getLength());
		PrincipalWindow.interpreter.requestFocusInWindow();
       // PrincipalWindow.panelConsole.revalidate();
        querying();
		
	}

	private void querying() {
		// TODO Auto-generated method stub
		PrincipalWindow.interpreter.setEditable(true);
		PrincipalWindow.interpreter.addKeyListener(new RunProgram());
	}

}*/
